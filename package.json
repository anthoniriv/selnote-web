{
  "name": "selnote-web",
  "private": true,
  "version": "0.0.0",
  "description": "Repository governance and offline validation scripts for Selnote.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "test": "node --test tests/check-repository.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
