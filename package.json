{
  "name": "noteschain-web",
  "private": true,
  "version": "0.0.0",
  "description": "Repository governance and offline validation scripts for NotesChain.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check-repository.mjs",
    "test": "node --test tests/check-repository.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
